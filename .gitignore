.DS_Store
__pycache__/
*.pyc
.venv/
venv/

# Raw Aria capture recordings — hundreds of MB, exceed GitHub's 100MB
# file limit, and are session data rather than source. Keep the small
# .vrs.json metadata sidecars, drop the recordings themselves.
sessions/*.vrs
